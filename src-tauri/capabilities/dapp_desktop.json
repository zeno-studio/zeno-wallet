{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dapp-desktop",
  "description": "Single reusable DApp browser window (desktop only)",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["dapp"],                   
  "permissions": [
    "core:app:default",
    "core:event:default"

  ]
}